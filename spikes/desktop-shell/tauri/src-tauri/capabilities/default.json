{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Narrow shell-spike capability. Project filesystem access remains behind Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
